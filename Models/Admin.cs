class Admin : Man
{
    int adminId;
    
    // functions 
    public Admin(string name, string userName, string password, int boy, int wight, int age, string cotchName)
        : base(name, userName, password, boy, wight, age, "admin")
    {
        
    }
    // list users
    // list cotch
    // list ...
    // send discound
}