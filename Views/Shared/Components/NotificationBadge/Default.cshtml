@model int

@if (Model > 0)
{
    <span class="position-absolute translate-middle badge rounded-pill bg-danger" style="top: 10px; right: -5px; font-size: 0.6rem;">
        @Model
        <span class="visually-hidden">unread messages</span>
    </span>
}
